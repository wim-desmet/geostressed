GeoStressed is a web-based interface to [[JMeter interface|JMeter testing]], to create GeoServer stress-suites more easily by having an opinionated view on how OGC web services should be tested. It simplifies the creation by using Geoserver's REST, WFS en WMS interfaces to query the structure and availability of layers, and to provide a user friendly interface to select from these values.

